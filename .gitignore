# 診斷與紀錄產物，不進版控
data/probe_report.txt
data/backfill_failures.csv
data/daily_log.txt
data/raw/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
Thumbs.db
desktop.ini
.DS_Store

# 注意：data/quotes/ 與 data/snapshots/ 是這個 repo 的重點，不要 ignore。
# 回補完約 4,900 個小 CSV，總量在 GitHub 免費額度內，不需要 LFS。
