# 診斷與紀錄產物，不進版控
data/probe_report.txt
data/daily_log.txt
data/raw/

# 驗證結果刻意進版控：異常清單要能追溯，且回測方法論會引用

# 注意：data/backfill_failures.csv 刻意「不」ignore。
# 第一次回補時把它擋掉，導致缺了三分之一的檔卻查不到失敗原因。

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
Thumbs.db
desktop.ini
.DS_Store

# 注意：data/quotes/ 與 data/snapshots/ 是這個 repo 的重點，不要 ignore。
# 回補完約 4,900 個小 CSV，總量在 GitHub 免費額度內，不需要 LFS。
