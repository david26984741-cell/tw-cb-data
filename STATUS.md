# 專案進度

最後更新：2026-08-26

---

## 一句話總結

櫃買那條線已經打通並開始自動抓取；MOPS 只摸清一半、還沒寫成程式；回測要用的加工資料一項都還沒做。

---

## ✅ 已完成

### 1. 重建專案（原本的整份專案檔遺失）

前一個 session 在雲端沙箱裡產出的 zip 從未送達本機，交接文件裡列的 `README.md`、`SETUP.md`、`PUSH.txt`、各支腳本全部不存在。手上只剩一份交接文件，而它明講「完整端點清單在 README，不在這裡重抄」—— 也就是所有實際網址都跟著弄丟了。

現在的專案是從零重建，端點是重新挖出來的。

### 2. 用瀏覽器從台灣 IP 實地挖出櫃買端點

無法靠推測，改用本機 Chrome 攔截官方前端的真實請求。取得：

- 索引 API 的完整參數（POST、`fileCode`、`date` 為西元）
- 六種報表的 `fileCode` 對照
- 靜態 CSV 的路徑規則（可由日期直接推導，免打 API）
- 檔案的真實格式（標籤式 CSV、Big5、一檔兩列）

全部寫進 `README.md` 第 1 節。

### 3. 寫出可用的抓取器並驗證

`scripts/tpex_cb.py` —— 索引、下載、解析、回補、每日更新。
解析結果實測：769 列 / 385 檔 / 空代號 0 列，中文正常。

### 4. 修掉兩個會讓程式完全跑不動的問題

- **SSL**：Python 3.13 起預設啟用嚴格憑證檢查，而櫃買憑證缺 `Subject Key Identifier`，導致所有連線失敗。只關閉該項檢查，憑證鏈與主機名驗證維持開啟。
- **編碼**：Windows 主控台 cp950 遇到無法對應的字元會讓整支程式崩潰。

### 5. 體檢腳本

`scripts/probe.py` —— 十項端點檢查，結果存成報告與原始回應檔。

### 6. 推上 GitHub 並建立自動化

repo：https://github.com/david26984741-cell/tw-cb-data

- `.github/workflows/backfill.yml`：手動觸發的歷史回補
- `.github/workflows/daily.yml`：每週一至五 18:30（台北）自動更新

### 7. 解決交接文件列為「最優先未解」的問題

**櫃買沒有封鎖 GitHub Actions 的 IP。** workflow 的連線體檢步驟通過，確定可以走雲端，不必退回本機或架 self-hosted runner。

---

## 🔄 進行中

### 歷史回補 —— 第一輪已跑完，但缺三分之一，需要第二輪

第一輪耗時 2 小時 10 分，抓到約 **3,144 檔，預期約 4,900**。

| 年 | 檔數 | | 年 | 檔數 |
|---|---|---|---|---|
| 2007 | 228 | | 2017 | 248 ✅ |
| 2008 | 230 | | 2018 | 249 ✅ |
| **2009** | **0** | | **2019** | **0** |
| 2010 | 108 | | 2020 | 247 ✅ |
| 2011 | 142 | | 2021 | 246 ✅ |
| 2012 | 147 | | 2022 | 205 |
| **2013** | **22** | | 2023 | 200 |
| 2014 | 66 | | 2024 | 200 |
| 2015 | 246 ✅ | | 2025 | 204 |
| 2016 | 246 ✅ | | 2026 | 138（年中）|

**原因是程式的缺陷，不是櫃買沒有資料。** 2018 與 2020 完整而 2019 掛零，不可能是來源缺漏。第一版的索引請求一旦失敗就整個月跳過，而且沒有重試 —— 兩小時內五千次請求，偶發失敗是必然的。

另外 `.gitignore` 誤把失敗清單擋掉，導致失敗原因查不到。

**已修正**：

- 索引與下載都加上指數退避重試（最多 5 次）
- 下載後檢查檔頭，收到非報表內容（錯誤頁）也會重試
- 失敗清單改為進版控
- 加 `-u` 讓執行中就看得到逐月進度

**下一步**：重跑一次回補。已抓到的會自動略過，只補缺的，所以第二輪會比第一輪快很多。

---

## ❌ 尚未進行

依重要性排序。

### 1. 資料正確性驗證

**在這關通過前，不要拿這批資料做任何回測。** 抓得到不等於抓得對；格式解錯、缺漏未察、或誤把下櫃當成資料遺失，都會安靜地污染回測結果。

規劃寫成 `scripts/validate.py`，分五層，由淺入深。

#### 第 1 層 — 檔案完整性

- 檔數是否等於索引 API 列出的交易日數（不是「看起來差不多」，要逐日比對）
- 有無整段月份缺漏
- 檔案大小離群值：明顯過小的多半是休市日、半日交易，或抓到錯誤頁面
- 回補失敗清單（`data/backfill_failures.csv`）是否已清空

#### 第 2 層 — 結構正確性

- 每檔債券是否恰好兩列（等價／議價）；不是兩列就代表解析或來源有變
- 代號與名稱的前向填補是否正確 —— 空代號列數必須為 0
- 欄數是否逐日一致（櫃買改版會從這裡先露出來）
- 中文有無亂碼（Big5 解碼失敗的徵兆）

#### 第 3 層 — 報價擷取正確性

**不要用價格區間判斷合理性。** CB 的價格分布本來就極寬：深度價內可以到五、六百，瀕臨違約的剩五十。用區間篩選只會把最有資訊量的個券當成髒資料丟掉，而且完全抓不到真正的問題。

要驗的是**擷取與解析有沒有錯**，方法分兩種。

##### 3a. 與價格水準無關的內部一致性檢查

這些檢查不管價格是 50 還是 600 都成立，能抓出欄位錯位、前向填補失敗、欄序改版：

- 最高 ≥ 開市、收市、均價 ≥ 最低（四者都必須落在當日高低區間內）
- 明日漲停 > 明日參價 > 明日跌停，且三者比例在同一天的所有個券間一致
- 明日參價與當日收市（或均價）的對應關係，全樣本應一致
- 有量無價、有價無量的列
- 議價列多數為空是正常的；若某天大量議價列突然有值，先確認是解析錯位還是市場真實現象

欄位一旦錯位，這些關係會整片崩掉，而且與價格高低無關 —— 這才是可靠的訊號。

##### 3b. 與外部來源逐筆比對

內部檢查只能證明「自洽」，不能證明「正確」。要證明正確只能對外比。

**來源優先序**（你電腦上已安裝的排前面）：

1. **TEJ**（`TEJPro`、`TEJ Smart Wizard`）—— 台灣最權威、且**含已下市個券**，正好補上我們最擔心的那批。若能匯出 CB 日行情，這是最佳解。
2. **XQ 全球贏家** —— 已安裝，可匯出歷史報價。
3. **群益超級贏家 / 新超級贏家** —— 你自家券商端，適合驗近期。
4. **cb168**（`output.xlsx`）—— 免費、含已下市 2,322 檔，但只有代號／名稱／最高／最低／振幅，**沒有收盤價**，只能做部分欄位與代號母體的比對。
5. 公開網站 —— 最後手段，歷史深度通常不足，且已下市個券多半查不到。

**抽樣設計**（比來源更重要）：

不能只抽近期或只抽正常價位。必須涵蓋：

- 各年份都要有（尤其 2007–2014 這批最舊、也最可能有格式差異的）
- **已下櫃個券**（這是整個專案存在的理由，卻最難驗）
- 極端價格個券（高於 300、低於 70 各抽幾檔）
- 有除權息、有轉換價調整的日子前後
- 零成交日與大量成交日各抽

比對欄位：收市、開市、最高、最低、成交量。**容差應該是零** —— 同一個交易日的官方報價不該有差異。有差異就代表其中一邊解析錯了，要逐筆查明，不能用「誤差在可接受範圍」帶過。

##### 需要你決定的事

3b 要用哪個來源、以及能不能從 TEJ 或 XQ 匯出歷史 CB 報價，得你來判斷 —— 我不知道你這兩套的授權範圍涵蓋到哪。告訴我能匯出什麼格式，我來寫比對程式。

#### 第 4 層 — 時間序列連續性

- 逐檔追蹤代號的出現與消失
- **關鍵是區分「下櫃」與「資料缺漏」**：真下櫃是永久消失且有對應的最後交易日；資料缺漏是中間斷一段又出現。兩者混淆會直接扭曲存活期間，回測報酬跟著失真。
- 名稱變更、代號重用（不同公司在不同年份用到同一代號）也要在這層抓出來

#### 第 5 層 — 跨來源對帳

- 我們抓到的代號聯集 vs 官方母體 2,412 檔 vs cb168 的 2,322 檔，三方比對
- 與已有的 `CBBIGD`（三大法人買賣 CB 日檔）代號名單交叉驗證
- 差異必須逐項有解釋，不能只給一個百分比就當作通過

#### 需要你確認的判斷基準

第 3 層的門檻我可以先給一組保守預設值，但**價格區間、漲跌幅比例這兩項最好由你定**，因為那牽涉你實際在做 CBAS 時對「異常」的認定，跟教科書值可能不同。

驗證結果會輸出成一份報告，異常個券逐檔列出，不做成只有一個「通過／不通過」的黑箱。

### 2. 下櫃事件的重建與保存

櫃買的下櫃頁實測只保留 **10 筆、涵蓋 16 天**。過去十九年的下櫃事件早已從該頁掉光。

- 往前：從日行情反推（某代號從日檔消失即視為下櫃）
- 往後：必須至少每週快照一次

兩件都還沒做。**這是目前流失速度最快的資料。**

### 3. MOPS 條款資料

目前狀態：

| 項目 | 狀態 |
|---|---|
| 主機、編碼（UTF-8，非 Big5） | 已確認 |
| 包裹下載參數（含關鍵的 `bond_kind=5,7`） | 已確認可用 |
| 113 欄的欄位對照 | 已記錄 |
| **只能取當月、無法回補歷史** | 已確認為限制 |
| `t120sg01`（條款歷史的正解） | **未打通**，回「年月參數錯誤」 |
| 抓取程式 | **完全未寫** |

### 4. 事件表

把逐月條款快照 diff 成事件表。**關鍵：用「最近轉(交)換價格生效日期」當事件時點，不是資料年月**——後者只是快照時間，混用會製造前視偏誤。

程式未寫，且上游的 MOPS 資料也還沒有。

### 5. 轉換價調整分類

反稀釋（除權息、現增，機械性）vs 向下重設（reset，帶資訊）。不分開會讓訊號被大量無意義的機械調整稀釋。

新發現：MOPS 包裹下載第 89、90 欄直接有「本次重設日期／下次重設日期」，可當主判準。
但那是當期快照，只給最近一次，**不是完整重設史**，所以仍需用標的股除權息日交叉驗證。標的股資料可接既有的 `daily-postmarket`。

### 6. 覆蓋率計算

實測 12211 久津一、15291 樂士一、62441 茂迪一 都查無條款資料。**這批正是最極端的樣本，缺了會系統性高估報酬。** 必須算出覆蓋率並寫進回測方法論。

### 7. 其他未挖端點

債息資料、賣回權資料、三大法人買賣 CB（明細與彙總）、轉換暨累計彙總表、停止轉換與變更交易公告。頁面網址都已定位（README 1.5 節），參數未抓、程式未寫。

---

## 對原交接文件的修正

驗證後確認有誤或不完整的部分。

| 項目 | 原文件 | 實測 |
|---|---|---|
| 行情 API 方法 | 未記載 | **POST**，用 GET 回「參數輸入錯誤」 |
| CSV 格式 | 未記載 | 每行開頭有 `TITLE`/`HEADER`/`BODY` 標籤，直接 `read_csv` 會壞 |
| 回補請求數 | 4,900 次逐日 | 236 次索引即可列出全部；`date` 選的是「月」不是「日」 |
| MOPS 編碼 | Big5 | **UTF-8** |
| 包裹下載欄數 | 132 欄 | **113 欄** |
| 包裹下載篩選 | 未記載 | 少了 `bond_kind=5,7` 會混進普通公司債與金融債券 |
| `t120sg01` 錯誤訊息 | 「債券期參數錯誤」 | 實際是「**年月參數錯誤**」；`bond_yrn`/`bond_subn` 兩種送法回應 byte 完全相同，代表該規則無法確認 |
| 下櫃頁視窗 | 未量化 | 僅 10 筆 / 16 天 |
| Actions IP 是否被擋 | 未知，列為最優先 | **未被擋**，已實測通過 |

已驗證為真的部分：Big5（櫃買行情檔）、一檔兩列且代號只在第一列、歷史起點 2007-01-02、`step=12`、「除預設項目外，請至少輸入一項查詢項目」、櫃買無 robots.txt。

---

## 建議的下一步順序

1. 等回補跑完 → **跑完整的五層資料正確性驗證**（第 1 項）
2. 做下櫃快照，止血（第 2 項）
3. 打通 `t120sg01` 的年月參數（第 3 項）
4. 條款回補 → 事件表 → 覆蓋率（第 4、6 項）

第 1 項與第 2 項有重疊：驗證的第 4 層（區分下櫃與資料缺漏）產出的結果，正好就是重建下櫃歷史所需要的清單。兩件事可以一起做。
